apiVersion: apps/v1
kind: Deployment
metadata:
  name: boot-to-k8s-deployment
spec:
  template:
    spec:
      containers:
        - name: boot-to-k8s
          resources:
            limits:
              memory: 1Gi
            requests:
              memory: 1Gi
